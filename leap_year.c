#include<stdio.h>
int main()
{
   int year;
   printf("Enter year");
   scanf("%d",&year);
if (year%4=0)
{
    printf("year %d",year" is not leap year ");
}
else()
printf("year %d",year" is not leap year ");
    return 0;
}
